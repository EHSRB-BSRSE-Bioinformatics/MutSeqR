# Characterize Variants

This function generates additional columns for the mutation data,
including a breakdown of the mutation subtypes at various resolutions.

## Usage

``` r
characterize_variants(mutation_data)
```

## Arguments

- mutation_data:

  A data frame containing mutation data.

## Value

A data frame with additional columns for variant characterization.

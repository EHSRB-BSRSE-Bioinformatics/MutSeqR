# Get Mutation Palette

Internal helper function to determine the color palette based on
resolution. Supports types, base_6, base_12, base_96, base_192

## Usage

``` r
get_mutation_palette(custom_palette = NULL, subtype_resolution, num_colours)
```

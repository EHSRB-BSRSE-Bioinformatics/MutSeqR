# Set up Python environment for MutSeqR

This function initializes the Python environment used by MutSeqR. It is
not run automatically to avoid issues during installation and checks.

## Usage

``` r
setup_mutseqr_python(force = FALSE)
```

## Arguments

- force:

  Logical. Whether to force reconfiguration even if an environment
  already exists.

## Value

None
